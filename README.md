# FamilyOS releases

Built artifacts for the FamilyOS desktop app (source lives in a private
repo). Fresh installs: grab the `.dmg` from the latest release.
Installed apps update themselves via `latest.json`.
